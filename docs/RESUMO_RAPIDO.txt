# 🎯 RESUMO EXECUTIVO — SPRINT 4 (até 17/05)

## 📊 STATUS ATUAL (06/05)

- ✅ Exceptions + mapper global: **feito**
- ✅ DTOs de criação/atualização: **feito**
- 🟡 Controllers CRUD: **parcial** (tem estrutura, mas ainda faltam PUTs e padronização)
- 🔴 Config Oracle via env vars: **falta** (ainda está hardcoded)
- 🔴 4 métodos de negócio obrigatórios (Sprint 3 → Service): **faltam**

---

## ✅ PRONTO / ADIANTADO (o que já melhorou bastante)

- Camadas organizadas (controller/service/repository/model/exception)
- Exceptions + `ExceptionsMapperGlobal` retornando `ErroDTO` (404/422/409/400)
- DTOs separados para criação e atualização (`Criar*DTO` / `Atualizar*DTO`)
- Swagger/OpenAPI presente e prints iniciais em `docs/prints_swagger/`
- Dependências principais ok no `pom.xml` (REST, OpenAPI, Panache, Oracle, Validator)

---

## 🚨 FALTA (bloqueadores de nota)

1) **Config Oracle via env vars** (requisito explícito)
   - `quarkus.datasource.username=${DB_USER}`
   - `quarkus.datasource.password=${DB_PASSWORD}`
   - `quarkus.datasource.jdbc.url=${DB_URL:jdbc:oracle:thin:@//oracle.fiap.com.br:1521/orcl}`

2) **4 métodos de negócio obrigatórios (Sprint 3 → Service)**
   - `BeneficiarioService.adicionar(idPedido, idProgramaSocial)`
   - `DentistaService.validarCro(cro)` (lançando `ValidacaoException` quando inválido)
   - `PedidoAjudaService.processarPedido(id, novoStatus, idDentista)`
   - `AtendimentoService.encerrar(id, prontuario, dataFinal, idColaborador)`

3) **CRUD completo e padronizado**
   - preencher `PUT` que está comentado (ex.: `PedidoAjudaController`)
   - garantir `Response` com status corretos (201/200/204/404/422/409)
   - usar DTOs também nos `PUT` (evitar update sem request)

4) **Evidências / PDF final**
   - pelo menos 1 print por método obrigatório (sucesso + erro)
   - tabela de endpoints + instruções de execução

---

## 🎯 PRÓXIMAS AÇÕES (ordem sugerida)

**Hoje / próximo commit**
- [ ] Ajustar `application.properties` para env vars + flags do Hibernate/Swagger
- [ ] Rodar `./mvnw compile`

**Depois (fechar o coração da Sprint 4)**
- [ ] Implementar os 4 métodos de negócio (services + endpoints)
- [ ] Completar CRUD (principalmente PUT) e alinhar validações/exceções
- [ ] Atualizar `docs/PROGRESSO_SPRINT4.md` com os prints novos

---

## 🧾 Onde provar (e organizar para a equipe)

- Checklist/placar: `docs/STATUS_SPRINT4.md`
- Evidências (prints + rotas): `docs/PROGRESSO_SPRINT4.md`
- Prints Swagger: `docs/prints_swagger/`
